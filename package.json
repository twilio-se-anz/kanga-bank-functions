{
  "name": "kanga-bank-functions",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "@prisma/client": "^3.15.1",
    "@twilio/runtime-handler": "1.2.3",
    "axios": "^0.27.2",
    "twilio": "^3.56"
  },
  "devDependencies": {
    "prisma": "^3.15.1",
    "twilio-run": "^3.4.2"
  },
  "engines": {
    "node": "14"
  }
}
